@model IEnumerable<Website_THPT.Models.TINTUC>
@{
    ViewBag.Title = "QUẢN LÝ TIN TỨC";
    Layout = "~/Areas/Admin/Views/Shared/Admin_Layout.cshtml";
}

<h2 style="text-align: center">QUẢN LÝ TIN TỨC HOẠT ĐỘNG</h2>
<a href="@Url.Action("Create", "HoatDong")" class="btn btn-success" role="button" style="margin-bottom: 5px;">
    Thêm
</a>
<table class="table">
    <tr>
        <th>
            Tên Tin
        </th>
        @*<th>
                Nội dung
            </th>*@
        <th>
            Ngày Tạo
        </th>

        <th>
            Chức Năng
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenTin)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.NoiDung)
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.NgayTao)
            </td>

            <td>
                <a href="@Url.Action("Edit", new { id = item.Ma })" class="btn btn-warning" role="button"><i class="fa-regular fa-pen-to-square"></i></a>
                <a href="@Url.Action("Delete", new { id = item.Ma })" class="btn btn-danger" role="button"><i class="fa-solid fa-trash"></i></a>
            </td>
        </tr>
    }
</table>

@section NavLeft_Partial{
    @Html.Action("NavLeft_Partial", "Home")
}